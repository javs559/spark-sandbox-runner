{
  "name": "spark-sandbox-runner",
  "version": "1.0.0",
  "private": true,
  "description": "Railway-ready Spark tool service with HTTP fetch, title fetch, crawl, and optional SOCKS5 proxy support.",
  "main": "server.js",
  "type": "commonjs",
  "scripts": {
    "start": "node server.js",
    "dev": "node server.js",
    "test": "node -e \"require('./server'); console.log('server loads')\""
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "axios": "^1.7.9",
    "cheerio": "1.0.0-rc.12",
    "cors": "^2.8.5",
    "express": "^4.18.3",
    "socks-proxy-agent": "^8.0.5"
  }
}
